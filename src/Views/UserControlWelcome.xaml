<UserControl
    x:Class="MagonoteToolkitForEmbedded.Views.UserControlWelcome"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:p="clr-namespace:MagonoteToolkitForEmbedded.Properties"
    xmlns:prism="http://prismlibrary.com/"
    xmlns:viewmodels="clr-namespace:MagonoteToolkitForEmbedded.ViewModels"
    d:DataContext="{d:DesignInstance Type=viewmodels:UserControlWelcomeViewModel,
                                     IsDesignTimeCreatable=True}"
    prism:ViewModelLocator.AutoWireViewModel="True"
    mc:Ignorable="d">
    <Grid Margin="10,10,10,10">
        <StackPanel>
            <TextBlock Style="{StaticResource MaterialDesignHeadline5TextBlock}" Text="{x:Static p:Resources.MessageWelcomeTitle}" />
            <TextBlock Style="{StaticResource MaterialDesignBody1TextBlock}" Text="{x:Static p:Resources.MessageWelcomeBody}" />
        </StackPanel>
    </Grid>
</UserControl>
